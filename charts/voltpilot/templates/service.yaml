apiVersion: v1
kind: Service
metadata:
  name: {{ include "voltpilot.fullname" . }}
  labels:
    {{- include "voltpilot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "voltpilot.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
